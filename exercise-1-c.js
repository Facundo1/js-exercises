// 1) Variables and Operators

/* c) Create two variables of type String and add the length of each variable (number of letters
del string) saving the result the sum of a 3rd variable (use length). */

console.log('c)');
var string1 = 'text number one';
var string2 = 'text number two';
var stringLengthSum = string1.length + string2.length;
console.log('the first string length is:', string1.length);
console.log('the second string length is:', string2.length);
console.log('the sum of the length of each variable is', stringLengthSum);
console.log('___________________///////////_____________________');
